{% extends 'base.html.twig' %}

{% block title %}Erreur
{% endblock %}

{% block body %}
	<div class="flex flex-col justify-center mt-32 space-y-6">
		<h1 class="text-center text-4xl font-bold text-indigo-500 font-sans leading-9 tracking-tight">
			Une erreur est survenue
		</h1>
		{% for message in app.flashes('error') %}
			<div class="text-indigo-500">
				{{ message }}
			</div>
		{% endfor %}
		<p class="text-center text-indigo-500 font-sans">
			<a href="{{ path('app_recette') }}">Retour à la page d'accueil</a>
		</p>
	</div>
{% endblock %}
